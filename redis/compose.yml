version: '3.9'
services:
  cache:
    image: redis:7.2-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning --requirepass foofar
    volumes: 
      - cache:/data
volumes:
  cache:
    driver: local
