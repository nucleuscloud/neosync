input:
  pooled_sql_raw:
    driver: postgres
    dsn: postgres://postgres:foofar@localhost:5434/nucleus?sslmode=disable
    query: SELECT "id", "myjson" FROM "public"."unusual"
output:
  label: ""
  pooled_sql_insert:
    driver: postgres
    dsn: postgres://postgres:foofar@localhost:5435/nucleus?sslmode=disable
    schema: public
    table: unusual
    columns: ["id", "myjson"]
    args_mapping: root = [this.id, this.myjson]
