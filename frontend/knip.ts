import type { KnipConfig } from 'knip';

const config: KnipConfig = {
  ignore: ['next-env.d.ts', 'next.config.mjs', 'neosync-api-client', 'components/ui', 'postcss.config.cjs'],
};

export default config;
