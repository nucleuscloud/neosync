export { auth as default } from './app/api/auth/[...nextauth]/auth';
// const method: NextMiddleware = (req) => {
//   return auth(req, NextResponse.next())
//   return NextResponse.next();
// };

// export default method;
// export default auth((req) => {})
// export const config = {
//   matcher: ['/'],
// };
