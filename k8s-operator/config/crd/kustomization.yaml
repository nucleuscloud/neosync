# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/neosync.dev_connections.yaml
- bases/neosync.dev_jobs.yaml
- bases/neosync.dev_jobruns.yaml
- bases/neosync.dev_sqlconnections.yaml
- bases/neosync.dev_tasks.yaml
- bases/neosync.dev_jobconfigs.yaml
- bases/neosync.dev_taskruns.yaml
- bases/neosync.dev_awss3connections.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_connections.yaml
#- patches/webhook_in_jobs.yaml
#- patches/webhook_in_jobruns.yaml
#- patches/webhook_in_sqlconnections.yaml
#- patches/webhook_in_tasks.yaml
#- patches/webhook_in_jobconfigs.yaml
#- patches/webhook_in_taskruns.yaml
#- patches/webhook_in_awss3connections.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_connections.yaml
#- patches/cainjection_in_jobs.yaml
#- patches/cainjection_in_jobruns.yaml
#- patches/cainjection_in_sqlconnections.yaml
#- patches/cainjection_in_tasks.yaml
#- patches/cainjection_in_jobconfigs.yaml
#- patches/cainjection_in_taskruns.yaml
#- patches/cainjection_in_awss3connections.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
