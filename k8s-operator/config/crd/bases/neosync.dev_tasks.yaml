---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: tasks.neosync.dev
spec:
  group: neosync.dev
  names:
    kind: Task
    listKind: TaskList
    plural: tasks
    singular: task
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Task is the Schema for the tasks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TaskSpec defines the desired state of Task
            properties:
              runConfig:
                description: Represents the run config. Currently benthos is the supported
                  provider
                properties:
                  benthos:
                    description: Represents the configuration needed to spawn a benthos
                      sync process
                    properties:
                      configFrom:
                        description: Specify where to pull the benthos.yaml config
                          from
                        properties:
                          secretKeyRef:
                            description: Secret key reference of where the benthos.yaml
                              file lives
                            properties:
                              key:
                                description: The key to select from the resource
                                type: string
                              name:
                                description: The name of the resource to be selected
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        required:
                        - secretKeyRef
                        type: object
                      image:
                        description: Optionally provide an alternative image to run
                          benthos. Useful if augmenting benthos to provide custom
                          plugins, or to pull from an alternative registry
                        type: string
                    required:
                    - configFrom
                    type: object
                required:
                - benthos
                type: object
            required:
            - runConfig
            type: object
          status:
            description: TaskStatus defines the observed state of Task
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
